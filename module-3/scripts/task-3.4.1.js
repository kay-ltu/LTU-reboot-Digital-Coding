// loop the 7 times table, on the proviso of 7 x 1, 7 x 2 to a max of 12 x 7, the var would be 1 as next it would be 2 and so on.
for (var sevenTimes = 1; sevenTimes <=12; sevenTimes++ )
{
    var number = 7;

    var result = sevenTimes * number;

        console.log( number + ' * ' + sevenTimes + ' = ' + result );
}

